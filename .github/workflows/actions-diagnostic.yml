name: Actions Diagnostic

on:
  workflow_dispatch:
  pull_request:
    branches: [main]
    paths:
      - ".github/workflows/actions-diagnostic.yml"

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-24.04
    steps:
      - name: Runner smoke test
        run: |
          echo "RUNNER_OK"
          uname -a
          free -h || true
